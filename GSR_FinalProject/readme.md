# L-SNMPvS Monitoring System

## How to Run

1.  **Start the Agent (Server)** in one terminal:
    ```bash
    python -m Agent.udp_server
    ```

2.  **Start the Manager (GUI)** in another terminal:
    ```bash
    python -m manager.LSNMPManagerGUI
    ```
